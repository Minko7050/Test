library ieee;
use ieee.std_logic_1164.all;
entity halfadder is
port(x,y:in bit;sum,carry:out bit);
end halfadder;
architecture behavior of halfadder is 
begin 
sum<=x xor y;
carry<=x and y;
end behavior;
